;(function($){
	$.fn.table=function(options){
		var defaults = {
			//各种参数 各种属性
			
			


		}
		var options = $.extend(defaults,options);//options继承了defaults中的属性

		this.each(function(){
			//实现功能的代码
		

		});
	}
})(jQuery);  